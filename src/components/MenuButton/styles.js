import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  btnClickContain: {
    flexDirection: 'row',
    padding: 4,
    marginBottom: 12,
    borderRadius: 4,
  },
  btnContainer: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
  },
  btnIcon: {
    fontSize: 24,
    color: '#5E629C',
  },
  btnText: {
    fontSize: 18,
    marginLeft: 16,
    marginTop: 2,
    color: '#D5D7EF',
  },
});

export default styles;
